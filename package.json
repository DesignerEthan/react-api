{
  "name": "web-projects",
  "version": "1.0.0",
  "description": "List of web projects",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend",
  "keywords": [
    "json",
    "api",
    "projects"
  ],
  "author": "Ethan Ellis",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "react-uuid": "^1.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": "14.15.4",
    "npm": "6.14.10"
  }
}
